<div id="wrapper">

  <%=render template:  "shared/nav_dashboard"%>

  <!-- /#sidebar-wrapper -->

  <!-- Page Content -->
  <div id="page-content-wrapper">

    <div class="row">
      <div class="col-xs-12 icon-nav-profile">
        <a id="menu-toggle">
          <i class="fa fa-bars fa-2x" aria-hidden="true"></i>
        </a>
      </div>
      <div class="col-xs-6">
        <%if current_user.avatar.present?%>
          <%= image_tag(current_user.avatar.thumb.url, :class => "img-rounded image-avatar-profile")%>
        <%end%>
        <h1 class="title-name-profile">Manuel Gómez</h1>
      </div>
      <div class="col-xs-6">
        <h1 class="title-num-albums">Albumes totales</h1>
        <p class="num-albums">23</p>
      </div>
    </div>

    <div class="row">
      <div class="col-xs-12">

        <div class="container">
          <%if @albums.blank?%>
          <h2>Mis albums</h2>
          <h4 >No tienes albumes. Puedes <%=link_to "añadir uno ahora", new_album_path	%></h4>
          <%else%>
          <p>Lista de albums añadidos:</p>
          <table class="table table-hover">
            <thead>
              <tr>
                <th>name</th>
                <th>Fecha de creación</th>
              </tr>
            </thead>
            <tbody>
              <%@albums.map do |album| %>
              <tr>
                <td><%=album.name.truncate(10)%></td>
                <td><%=album.created_at%></td>
              </tr>
              <%end%>
              <%end%>
            </tbody>
          </table>
        </div>
      </div>

    </div>

  </div>
  <!-- /#page-content-wrapper -->

</div>
<script>
</script>

<!-- /#wrapper -->
